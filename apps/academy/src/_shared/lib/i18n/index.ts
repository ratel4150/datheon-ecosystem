// PROVISIONAL — stub. AcademyHero resuelve su propio contenido
// internamente (resolveLang + content.ts por feature) y no depende de
// este objeto todavía. Reemplazar cuando se comparta el i18n real de
// landing-page.
export function getTranslation(_lang: string): Record<string, unknown> {
  return {};
}
