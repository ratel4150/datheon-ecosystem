---
// _pages/home/ui/HomePage.astro
import type { Translations } from '@/_shared/types/i18n';

export interface Props {
  lang: string;
  t: Translations;
}

const { lang, t } = Astro.props;
---

<div class="min-h-screen flex items-center justify-center bg-[#0B0F2B]">
  <div class="text-center">
    <h1 class="text-4xl font-bold text-white mb-4">
      {t?.hero?.title || 'Bienvenido a Datheón'}
    </h1>
    <p class="text-gray-300">Idioma actual: {lang}</p>
    <!-- Aquí irá el Hero Section y demás contenido -->
  </div>
</div>